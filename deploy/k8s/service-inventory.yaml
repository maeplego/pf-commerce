apiVersion: v1
kind: Service
metadata:
  name: inventory
  labels:
    app: commerce-inventory
spec:
  selector:
    app: commerce-inventory
  ports:
    - name: http
      port: 8102
      targetPort: 8102
