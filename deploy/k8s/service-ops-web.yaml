apiVersion: v1
kind: Service
metadata:
  name: ops-web
  labels:
    app: commerce-ops-web
spec:
  selector:
    app: commerce-ops-web
  ports:
    - name: http
      port: 3010
      targetPort: 3010
