apiVersion: v1
kind: Service
metadata:
  name: payment
  labels:
    app: commerce-payment
spec:
  selector:
    app: commerce-payment
  ports:
    - name: http
      port: 8104
      targetPort: 8104
